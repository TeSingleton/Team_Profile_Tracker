


// View all departments 
// SELECT * FROM Departments 

// View all roles 

// Select * From roles

// View all employees 

// Select * From employee

// Create new departments 


// Prompt user for the name of the department. 

        // THEN run the query
        // INSERT INTO Departments(name)
        // VALUES ("Sales");
                    // THEN ask the user what they want to do next 

// Create a new role 

// Get the existing Departments from the 'department table 

        // THEN // prompt the user for the title . salray or department for the role. 

            // THEN Run the query
            // INSERT INTO role (title , salary, department_id)
            //VALUES ("** ROLES ", 120000 ,1 )

                                // THEN ask the user5 what they want to do next?